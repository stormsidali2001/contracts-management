export enum AgreementStatus{
    NOT_EXECUTED = 'not executed',

    IN_EXECUTION_WITH_DELAY = 'in execution with delay',
    IN_EXECUTION = 'in execution',
    
    EXECUTED_WITH_DELAY = 'executed_with_delay',
    EXECUTED = 'executed',
}